GITHUB_TOKEN=your_github_personal_access_token_here
PORT=3000

# API Cache Duration in milliseconds (default: 600000 = 10 minutes)
# This controls how long GitHub API responses are cached
# Increase this value to reduce API calls and avoid rate limits
# Decrease to get more fresh data
API_CACHE_DURATION=600000
